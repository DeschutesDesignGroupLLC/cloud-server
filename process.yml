apps:
  - script: server.js
    name: parse-server
    instances: max
    exec_mode: cluster
    cwd: /opt/deschutesdesigngroup/apps/parse/htdocs
    autorestart: true
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
